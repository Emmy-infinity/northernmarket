name: Keep Render App Alive
on:
  schedule:
    - cron: '*/5 * * * *'   # every 5 minutes
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -s https://django-ecommerce-backend-4u8q.onrender.com
